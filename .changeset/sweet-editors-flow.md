---
"@railway/inkwell": minor
---

Expose `useInkwell`, a hook-style editor API that returns `{ state, EditorInstance, editor }`, plus a low-level editor handle, read-only mode, and state callbacks for focus, clearing, replacing Markdown, insertion, and state inspection.
